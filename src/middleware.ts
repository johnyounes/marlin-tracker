// AUTH TEMPORARILY DISABLED FOR TESTING
// export { default } from 'next-auth/middleware'
// export const config = {
//   matcher: ['/dashboard/:path*', '/api/scores/:path*', '/api/trips/:path*'],
// }
export const config = { matcher: [] }
